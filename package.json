{
  "name": "@lenticule-science/cover-nerve-rank-readout-service",
  "version": "1.0.0",
  "description": "Placement-independent CoverNerve H0/H1 logic die with a canonical typed RMN pinout.",
  "type": "module",
  "license": "MIT",
  "files": [
    "LICENSE",
    "README.md",
    "src/"
  ],
  "exports": {
    ".": "./src/index.mjs",
    "./function": "./src/cover-nerve.mjs",
    "./attestation": "./src/source-attestation.mjs"
  },
  "scripts": {
    "check": "node --check src/*.mjs",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run check && npm test && npm pack --dry-run"
  },
  "dependencies": {
    "@red-cup-engineering/relation-model-notation-runtime": "7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
